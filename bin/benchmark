#!/usr/bin/env ruby

require "bundler/setup"
require 'byebug'

$: << File.expand_path(File.join(__FILE__, "..", "..", "lib"))

require "http-clients"
require 'optparse'

number     = 10
persistent = false

parser = OptionParser.new do |opts|
  opts.banner = "Usage: bin/benchmark [options] URL"

  opts.on("-n N", "--number N", Integer, "Run N requests") do |number|
    number = number
  end

  opts.on("-p", "--persistent", "Try to use a persistent connection if supported by the client") do
    persistent = true
  end
end

parser.parse!

endpoint = parser.default_argv.first

HTTPClients::Benchmark.new(endpoint, number: number, persistent: persistent).run
